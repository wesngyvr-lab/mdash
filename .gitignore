# Secrets — never commit
.env
.env.local
secrets/

# Live launchd plist (contains user-specific paths)
launchd/com.wesleyng.*.plist

# Runtime logs
launchd.log
launchd.err
*.log

# Node
node_modules/

# OS
.DS_Store
