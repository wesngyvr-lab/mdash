# Secrets — never commit
.env
.env.local
secrets/

# Live launchd plist (contains user-specific paths)
# Live launchd plists (only the .template file should be committed)
launchd/*.plist

# Runtime logs
launchd.log
launchd.err
*.log

# Node
node_modules/

# OS
.DS_Store
